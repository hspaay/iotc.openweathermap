# Test yaml configuration for iotconnect messenger
# messenger: "DummyMessenger"
messenger: "MQTTMessenger"

server: "localhost"

login: ""
credentials: ""

# override the domain
domain: "test"
# "none" for testing, default is "jws"
signing: "none"   
